---
const { name, link } = Astro.props
---
<a href={link} class="nav-item" >
  {name}
</a>

<style>
  .nav-item {
      font-size: 18px;
      cursor: pointer;
      transition: var(--transition);

      &:hover, &:focus {
        color: var(--soft-red);
      }
    }
</style>